// import { Navigate, useNavigate } from "react-router-dom";

export default function NotFound() {
  // <Navigate to='куда отправить'/> = позволяет перенаправить пользователя на любую страницу
  // useNavigate = хук работает как же как и Navigate, navigate('путь'. { option })
  return <h1>notfound page</h1>;
}
